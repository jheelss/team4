package org.example.insurance.policy; import org.springframework.boot.*; import org.springframework.boot.autoconfigure.*;
@SpringBootApplication
public class PolicyApplication {
    public static void main(String[] a){
        SpringApplication.run(PolicyApplication.class,a);
    }
}
